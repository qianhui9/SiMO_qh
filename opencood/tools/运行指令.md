cd /data/qh/phdCode/work3/SiMO_qh
conda activate SiMO_qh

python -u opencood/tools/inference.py \
  --model_dir saved_models/SiMO-PF \
  --fusion_method intermediate \
  --range 51.2,51.2 \
  --save_vis_interval 1000

python -u opencood/tools/inference.py \
  --model_dir saved_models/SiMO-AttFuse \
  --fusion_method intermediate \
  --range 51.2,51.2 \
  --save_vis_interval 1000

如果要保存预测和 GT 的 .npy，加上：
--save_npy

如果要看更多可视化，把 --save_vis_interval 100000 改小，例如：
--save_vis_interval 10


注意：当前配置已设为 LiDAR+Camera 多模态推理。若要跑单模态，需要改对应 config.yaml 里的：

lamma:
  single_mode: lidar   # 或 camera；多模态为 false
然后重新运行同一条推理命令即可。